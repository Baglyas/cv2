import React from "react";
import { Box, Card, Typography } from "@mui/material";
const Picture = () => {
  return <Card sx={{  height: "100%" , backgroundColor: '#FFFFFF'}}></Card>;
};

export default Picture;
